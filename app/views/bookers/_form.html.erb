<%= form_with(model: booker, local: true) do |form| %>
  <% if booker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booker.errors.count, "error") %> prohibited this booker from being saved:</h2>

      <ul>
      <% booker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
